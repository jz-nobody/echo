# 2026-07-30

## 当前步骤
Phase 3 — 体验打磨 / Bug 修复

## 已完成
- **Codex 任务卡"运行中"修复**：Codex Agent 因网络错误退出后状态不再更新的问题。
  - `ProcessAncestry` 新增 `findAgentProcessPID(from:matching:)`，沿进程树定位 Codex CLI 进程 PID
  - `BridgeServer` 新增 `agentProcessPIDs` 追踪 + `cleanupStaleActiveSessions()`：活跃态 session 的 agent 进程死亡时转 idle，进程存活则不动（避免误判长任务）
  - Codex 补齐 `StopFailure` hook 类型
- **QoderWork hook 拦截所有工具调用修复**：bridge 在无法连接 socket 时回退输出 `{"decision":"ask"}`，QoderWork 不认识 `ask` 导致每次工具调用（含 AskUserQuestion）被挡下。
  - bridge fallback 统一改为 `{}`（中性放行）：BridgeCLI/main.swift、HookInstaller 内嵌脚本、Resources/agent-island-bridge
  - 运行时放弃路径（stale/timeout/session-ended）的 `decision:"ask"` 改为 `.empty`，顺带修复 Claude Code 上这些路径被误编码为 deny 的隐患
  - 已将修复后的 bridge 覆盖安装到 `~/.agent-island/bin/` 即时解封

## 遇到的问题
- 磁盘上安装的 bridge 二进制是旧版（仍吐 `ask`），需覆盖安装才能即时生效；正式发布走 DMG 重打包

## 做出的决策
- Codex 失败检测采用**进程存活检测**而非超时阈值，避免长时间正常运行的任务被误判为 idle
- 中性放行响应统一用空对象 `{}`（`.empty`），这是 QoderWork 正常状态 hook 已接受的格式

## 下一步
- 合并到 main，重新打 DMG 包
- 验证 DMG 安装后 Codex/QoderWork 行为

## 验证清单
- [x] Build 成功
- [x] 测试通过（365）
- [x] 符合验收标准
