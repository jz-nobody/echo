# 2026-06-05

## 当前步骤
Codex 集成 + 试用期锁 + DMG 打包

## 已完成
- Codex agent 完整接入：hook 路由、SQLite 会话发现、终端 PID 检测
- Codex 会话隐藏 "Allow Always" 按钮（不兼容 updatedPermissions）
- 修复 QoderWork 会话在"运行中/空闲"间跳动
- 修复 agent/terminal 标签显示：agent 统一显示 "Qoder"，terminal tag 始终可见
- 试用期锁系统：21 天试用 + 90 天激活 + SHA256 token 校验 + Keychain 防篡改
- 激活界面嵌入刘海面板，含右上角 X 关闭按钮
- 设置→通行证页面：状态展示 + 手动激活入口
- 预生成 50 个 token，哈希写入代码，明文存 .token-registry.csv
- Info.plist + build-dmg.sh 打包脚本
- 多选/自由文本 choice confirmation 支持

## 遇到的问题
- ceil 浮点精度问题导致试用天数计算 off-by-one，改用 floor 除法
- TrialManager deinit 访问 @MainActor 属性编译错误，改为 stopTimer() 方法

## 做出的决策
- Token 校验用 SHA256 哈希比对，代码中不存明文
- 安装日期存 Keychain 防篡改（删除应用也不会清除）
- 激活有效期 90 天，到期需重新激活
- 预生成批量 token 方案，一人一码

## 下一步
- 运行 build-dmg.sh 生成 DMG 包
- 端到端测试试用期锁流程
- 验证 DMG 安装后功能完整

## 验证清单
- [x] Build 成功
- [x] 测试通过（359 tests）
- [x] 符合验收标准
