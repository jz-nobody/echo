# Agent Island - 交互与视觉设计方案

> 版本：v1.0.0  
> 最后更新：2026-05-19  
> 设计参考：VibeIsland (vibeisland.app) 1:1 复刻风格

---

## 1. 设计原则

- **不打扰**：默认缩小态极致紧凑，不影响用户当前工作
- **即时可用**：需要用户操作时主动展开，操作完即收起
- **视觉融合**：与 macOS 刘海区视觉一体化，深色主题为主
- **一致性**：所有 Agent 的状态表达使用统一的视觉语言

---

## 2. 色彩系统

### 2.1 背景色
- 缩小态背景：纯黑 `#000000`（与物理刘海融合）
- 扩展态面板背景：深灰 `#1C1C1E`，圆角 12pt
- 卡片背景：`#2C2C2E`

### 2.2 状态色
- 空闲：灰色 `#8E8E93`
- 思考中/执行中：蓝色 `#0A84FF`
- 已完成：绿色 `#30D158`
- 等待确认：橙色 `#FF9F0A`
- 错误：红色 `#FF453A`

### 2.3 文字色
- 主文字：白色 `#FFFFFF`
- 副文字：灰色 `#8E8E93`
- 强调文字（Agent 名称/状态）：对应状态色
- 代码/命令：等宽字体，绿色 `#30D158`

### 2.4 标签色
- Claude 标签：`#D97757`（暖橙色）
- Codex 标签：`#10A37F`（绿色）
- QoderWork 标签：`#0A84FF`（蓝色）
- Gemini 标签：`#4285F4`（蓝色）
- 终端标签（iTerm/Terminal/Ghostty）：`#6E6E73`（中性灰）

---

## 3. 缩小态（Compact Bar）设计

### 3.1 布局
```
┌─────────────────────────────────────────────────────┐
│ [像素图标+动画] [状态文案]          [N 个会话] [时长] │
└─────────────────────────────────────────────────────┘
```

### 3.2 尺寸
- 高度：32pt（与刘海物理高度一致）
- 宽度：自适应内容，约 180-300pt
- 内边距：水平 12pt
- 圆角：与刘海边缘融合（左侧圆角 16pt，右侧 0pt 或配合系统）

### 3.3 像素图标
- 尺寸：16x16pt
- 风格：像素风/8-bit 风格小机器人头像
- 动画：
  - 空闲：静止
  - 思考中：头部左右微摆（0.8s 循环）
  - 执行中：身体像素块流动（0.5s 循环）
  - 已完成：变为绿色，停止动画
  - 等待确认：橙色脉冲（1s 循环）

### 3.4 文案规则
- 简洁模式：仅显示图标 + 会话数
- 详细模式：图标 + 状态文案 + 用户名 + 会话数 + 最近任务时长

### 3.5 参考效果
参见：`/Users/wm338658/Downloads/运行中.jpg`（运行中缩小态）  
参见：`/Users/wm338658/Downloads/已完成.jpg`（已完成缩小态）

---

## 4. 扩展态（Expanded Panel）设计

### 4.1 面板容器
- 从缩小态下方展开（视觉上像从刘海"拉下来"）
- 最大高度：560pt（可配置）
- 最大宽度：640pt（可配置）
- 背景：`#1C1C1E`，圆角 12pt（底部圆角）
- 阴影：`0 4px 24px rgba(0,0,0,0.5)`
- 展开动画：spring 动画，duration 0.3s

### 4.2 任务卡片设计

每个任务为一个卡片行：

```
┌──────────────────────────────────────────────────────────┐
│ ● [任务标题]                    [Agent标签] [终端标签] [时长] │
│   └ [当前执行步骤/最新工具调用]                              │
└──────────────────────────────────────────────────────────┘
```

- 卡片高度：约 44-60pt
- 左侧状态点：圆形 8pt，颜色对应状态
- 任务标题：SF Pro Text Medium, 13pt
- 子行（当前步骤）：SF Pro Text Regular, 11pt, 灰色
- 右侧标签：圆角胶囊形，背景 `#3A3A3C`，文字 12pt
- hover 时卡片背景高亮：`#3A3A3C`

### 4.3 完成通知卡片设计

任务完成时的展开内容：

```
┌──────────────────────────────────────────────────────────┐
│ [像素图标] [用户名] · [任务标题]    [自动批准×] [Agent] [终端] [时长] │
│ ─────────────────────────────────────────────────────── │
│ [像素图标] 你：[用户上一轮输入摘要...]                      │
│            [当前工具调用: Bash claude --help...]          │
└──────────────────────────────────────────────────────────┘
```

参见：`/Users/wm338658/Downloads/运行中hover.jpg`

### 4.4 多任务列表设计

参见：`/Users/wm338658/Downloads/多agent列表.JPG`（多任务列表视图）  
参见：`/Users/wm338658/Downloads/点击任务唤起agent.jpg`（点击跳转视图）

- 列表滚动：超过面板高度时可滚动
- 每个任务行间距：1pt 分割线（`#3A3A3C`）
- 已完成任务显示 "Done — click to jump" 绿色文案

---

## 5. 确认交互面板设计

### 5.1 权限审批面板

参见：`/Users/wm338658/Downloads/用户确认项.jpg`

```
┌──────────────────────────────────────────────────────────┐
│ ● Permission Request                                      │
│                                                          │
│ ⚠ Edit src/auth/middleware.ts                            │
│                                                          │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ 12  const verify = (token) =>                        │ │
│ │ 13 - jwt.verify(token);                              │ │（红色）
│ │ 13 + if (!token) throw new                           │ │（绿色）
│ │ 14 +   AuthError('missing');                         │ │（绿色）
│ │ 15 + return jwt.verify(token...                      │ │（绿色）
│ └──────────────────────────────────────────────────────┘ │
│ +3 -1                                                    │
│                                                          │
│ ┌────────────────────┐  ┌────────────────────────────┐  │
│ │     Deny  ⌘N       │  │       Allow  ⌘Y            │  │
│ └────────────────────┘  └────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

- 代码区域：等宽字体 SF Mono, 11pt
- 背景 `#2C2C2E`，圆角 8pt
- 行号：灰色
- 删除行：红色文字 `#FF453A`
- 新增行：绿色文字 `#30D158`
- 按钮：高度 44pt，圆角 8pt
  - Deny：深灰背景 `#3A3A3C`，白色文字
  - Allow：白色背景，黑色文字（更醒目，引导用户确认）

### 5.2 选择问题面板

参见：`/Users/wm338658/Downloads/用户确认项2.jpg`

```
┌──────────────────────────────────────────────────────────┐
│ 💬 Claude asks                                            │
│                                                          │
│ Which deployment target?                                  │
│                                                          │
│ ┌──────────────────────────────────────────────────────┐ │
│ │  ⌘1  Production                                      │ │
│ └──────────────────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────────────────┐ │
│ │  ⌘2  Staging                                         │ │
│ └──────────────────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────────────────┐ │
│ │  ⌘3  Local only                                      │ │
│ └──────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

- Agent 来源标签：彩色文字（如 "Claude asks" 使用青色）
- 问题标题：白色，SF Pro Text Semibold, 15pt
- 选项卡片：高度 44pt，背景 `#1E3A3A`（深青色调），圆角 8pt
- 快捷键图标：圆角方形小标签 `⌘1`，背景 `#3A3A3C`
- 选项文字：白色 SF Pro Text Regular, 14pt
- hover 时选项背景亮度+10%

---

## 6. 设置面板设计

### 6.1 窗口规格
- 尺寸：720 x 560pt（固定大小）
- 风格：macOS 系统设置风格（NSToolbarStyle）
- 左侧：导航列表，宽度约 200pt
- 右侧：对应设置内容

### 6.2 导航项
- 通用（齿轮图标）
- 集成（蓝色方块图标）
- 通知（红色铃铛图标）
- 显示（"大小"字样图标）
- 声音（喇叭图标）
- 用量（红色仪表图标）
- —— 分隔线 ——
- 快捷键（键盘图标）
- SSH 远程（地球图标）
- 实验室（橙色三角图标）
- —— 分隔线（品牌名称）——
- 通行证/许可证（盾牌图标）
- 关于（信息图标）

参见：`/Users/wm338658/Downloads/通用设置.jpg`、`/Users/wm338658/Downloads/通用设置2.jpg`

### 6.3 控件风格
- 开关：macOS 原生 Toggle 样式
- 滑块：蓝色轨道 + 圆形滑块
- 下拉选择：原生 Picker
- 所有文字：SF Pro Text，主标题 13pt，描述 11pt 灰色

---

## 7. 动效规范

| 动效场景 | 类型 | 时长 | 曲线 |
|---------|------|------|------|
| 面板展开 | 高度从 0 增长到目标值 | 300ms | spring(response: 0.3, dampingFraction: 0.85) |
| 面板收起 | 高度缩减到 0 | 250ms | easeOut |
| 状态切换（缩小态图标） | 渐变 + 缩放 | 200ms | easeInOut |
| 任务卡片出现 | 从右滑入 + 渐显 | 200ms | easeOut |
| 确认项切换（排队模式） | 内容淡出淡入 | 200ms | easeInOut（面板不收起） |
| hover 卡片高亮 | 背景色变化 | 100ms | linear |

---

## 8. 字体规范

| 用途 | 字体 | 字号 | 字重 |
|------|------|------|------|
| 面板标题 | SF Pro Text | 15pt | Semibold |
| 任务名称 | SF Pro Text | 13pt | Medium |
| 副文字/描述 | SF Pro Text | 11pt | Regular |
| 代码/命令 | SF Mono | 11pt | Regular |
| 状态文案（缩小态） | SF Pro Text | 12pt | Medium |
| 快捷键标签 | SF Mono | 10pt | Medium |
| 标签（Agent/终端） | SF Pro Text | 11pt | Medium |
