# Agent Island - 产品需求文档 (PRD)

> 版本：v1.0.0  
> 最后更新：2026-05-19  
> 状态：初始需求定义

---

## 1. 产品概述

### 1.1 产品名称
Agent Island（暂定）

### 1.2 一句话描述
一款嵌入 Mac 刘海区的多 AI Agent 状态聚合器，让用户在不切换窗口的情况下实时掌控所有 Agent 的运行状态并完成交互确认。

### 1.3 目标用户
使用多个 AI 编程 Agent（Claude Code、Codex、QoderWork、OpenCode 等）进行并行开发的 Mac 用户。

### 1.4 核心价值
用户在浏览网页、写文档、做其他工作时，无需打开各 Agent 的对话界面，即可通过刘海区实时查看所有 Agent 的执行状态、接收完成通知、完成审批确认操作。

---

## 2. 功能列表

### F-001 刘海区常驻显示（缩小态）

| 属性 | 描述 |
|------|------|
| 优先级 | P0 |
| 所属模块 | UI / 窗口管理 |

**功能描述：**
应用以紧凑条形（bar）嵌入 Mac 屏幕刘海区右侧（或左侧，用户可配置），与系统刘海视觉融合。始终显示在最顶层，不被其他窗口遮挡。

**显示内容：**
- Agent 状态图标（像素风动画）
- 状态文案：空闲中 / 思考中 / 执行中 / 已完成 / 等待确认
- 活跃会话数量
- 会话标题（详细模式下）

**状态图标规则：**
- 空闲中：灰色静止
- 思考中：蓝色闪烁/波浪动画
- 执行中：蓝色流动动画
- 已完成：绿色静止
- 等待确认：橙色脉冲

**验收标准：**
1. 应用启动后在刘海区右侧出现紧凑 bar，高度与刘海一致（约 32pt）
2. bar 不遮挡系统菜单栏项目
3. 状态图标和文案根据当前 Agent 状态实时变化，延迟 < 3 秒
4. 当有多个 Agent 活跃时，显示优先级最高的状态（等待确认 > 执行中 > 思考中 > 空闲）
5. 支持"简洁"和"详细"两种显示模式切换

---

### F-002 面板展开（扩展态）

| 属性 | 描述 |
|------|------|
| 优先级 | P0 |
| 所属模块 | UI / 交互 |

**触发条件：**
- 鼠标 hover 到缩小态区域上方，延迟 0.15s（可配置）后展开
- Agent 任务完成并返回需要确认的内容时自动展开（可被智能抑制规则覆盖）

**面板内容：**
- 任务列表：所有活跃 Agent 的当前任务
- 每个任务卡片：
  - Agent 产品名称标签（Claude / Codex / QoderWork）
  - 运行终端/客户端标签（iTerm / Terminal / Ghostty / QoderWork App）
  - 任务标题
  - 用户上一轮输入摘要（截断显示）
  - 当前执行状态 + 已运行时长
  - 当前正在执行的工具/步骤（如 "Bash: npm test"）

**收起条件：**
- 鼠标离开面板区域时自动收起
- 按 ESC 键收起
- 面板外点击收起（可配置）

**验收标准：**
1. hover 后延迟 0.15s 展开，展开动画流畅（< 300ms 完成）
2. 面板最大高度 560pt、最大宽度 640pt（可配置）
3. 任务列表实时更新，新任务出现时自动加入列表
4. 鼠标离开后面板收起，收起动画流畅
5. 展开时不影响下层窗口操作（面板区域外鼠标事件穿透）

---

### F-003 任务完成通知 + 排队确认机制

| 属性 | 描述 |
|------|------|
| 优先级 | P0 |
| 所属模块 | 核心交互 |

**功能描述：**
当任一 Agent 完成本轮任务并返回结果（或需要用户确认）时：
1. 缩小态自动变为扩展态
2. 展示：用户上一轮的输入、本轮 Agent 返回的内容摘要、待确认项
3. 用户点击确认项后，Agent 根据用户选择继续工作
4. 确认完成后自动收起为缩小态

**排队规则：**
- 当用户还未完成第一个 Agent 的确认操作时，其他 Agent 也返回了结果
- 按照弹出时间顺序排队
- 用户确认完第一个后，面板内容直接切换到第二个（面板保持展开，不做收起再展开的动效）
- 所有排队项确认完毕后，面板收起

**验收标准：**
1. 任务完成后 < 3 秒内自动展开面板
2. 面板展示用户上一轮输入（完整或前 100 字截断）
3. 面板展示 Agent 返回内容（精简摘要，不超过面板高度）
4. 确认项以可点击按钮/卡片形式展示
5. 点击确认后 Agent 收到响应并继续工作
6. 多个待确认项排队显示，切换时面板不收起不闪烁
7. 全部确认完毕后面板自动收起

---

### F-004 权限审批交互

| 属性 | 描述 |
|------|------|
| 优先级 | P0 |
| 所属模块 | 核心交互 |

**功能描述：**
Agent 请求文件编辑、命令执行等敏感操作权限时，面板展示：
- 操作类型标题（"Permission Request"）
- 操作详情（如 "Edit src/auth/middleware.ts"）
- 文件 diff 预览（红色删除行 / 绿色新增行）
- 操作按钮：Deny (Cmd+N) / Allow (Cmd+Y)

**验收标准：**
1. diff 使用等宽字体渲染，红/绿色高亮区分增删
2. Cmd+Y 和 Cmd+N 快捷键在面板展开时可用
3. 点击 Allow 后 Agent 执行该操作，面板收起或切换到下一个待确认项
4. 点击 Deny 后 Agent 收到拒绝信号

---

### F-005 选择类问题交互

| 属性 | 描述 |
|------|------|
| 优先级 | P0 |
| 所属模块 | 核心交互 |

**功能描述：**
Agent 提出选择类问题（如 "Which deployment target?"）时：
- 问题标题 + 来源 Agent 标签
- 选项列表（每个选项一个可点击的卡片/按钮）
- 每个选项支持 Cmd+数字 快捷键（Cmd+1, Cmd+2, Cmd+3...）

**验收标准：**
1. 选项列表清晰展示，每个选项可独立点击
2. 快捷键 Cmd+1/2/3 与选项对应正确
3. 选择后 Agent 收到选项值并继续执行
4. 支持最多 9 个选项（Cmd+1 到 Cmd+9）

---

### F-006 点击跳转到 Agent 会话

| 属性 | 描述 |
|------|------|
| 优先级 | P1 |
| 所属模块 | 交互 |

**功能描述：**
在扩展态的任务列表中，点击某个任务卡片：
- 自动激活该 Agent 所在的终端/IDE 应用
- 自动切换到该 Agent 的对应 tab/session
- 将该窗口带到屏幕前台

**验收标准：**
1. 点击后 < 1 秒内目标应用窗口出现在前台
2. 正确定位到对应的终端 tab/session（而非仅仅激活应用）
3. 提供"禁用点击跳转"的设置选项
4. 跳转后面板自动收起

---

### F-007 Agent 适配器（多 Agent 接入）

| 属性 | 描述 |
|------|------|
| 优先级 | P0 |
| 所属模块 | 后端 / 适配层 |

**支持的 Agent：**

| Agent | 接入方式 | MVP 优先级 |
|-------|---------|-----------|
| QoderWork | MCP HTTP API (127.0.0.1:52345) | Phase 1 |
| Claude Code | 终端日志/hook 监听 | Phase 2 |
| Codex | 终端日志/hook 监听 | Phase 2 |
| OpenCode | 终端日志/hook 监听 | Phase 3 |
| Gemini CLI | 终端日志/hook 监听 | Phase 3 |

**适配器统一接口定义：**
每个 Agent 适配器必须实现以下能力：
- `discoverSessions()` → 发现当前活跃会话列表
- `getSessionStatus(id)` → 获取会话当前状态
- `getPendingConfirmations(id)` → 获取待确认项
- `respondConfirmation(id, response)` → 提交用户确认
- `sendMessage(id, text)` → 发送消息
- `cancelTask(id)` → 终止任务
- `jumpToSession(id)` → 跳转到会话

**验收标准：**
1. 适配器接口抽象完整，新增 Agent 只需实现协议
2. QoderWork 适配器通过 MCP API 完整工作
3. Claude Code 适配器通过终端 hook 完整工作
4. 多个 Agent 同时活跃时互不干扰

---

### F-008 智能抑制

| 属性 | 描述 |
|------|------|
| 优先级 | P1 |
| 所属模块 | 交互逻辑 |

**功能描述：**
当 Agent 所在终端标签页已在用户前台（即用户正在看着 Agent 输出）时，不自动展开面板——因为用户已经能直接看到状态。

**验收标准：**
1. 检测到当前前台窗口是 Agent 所在终端时，抑制自动展开
2. 手动 hover 仍可触发展开（不抑制手动操作）
3. 切换到其他应用后恢复自动展开行为
4. 提供开关：可关闭智能抑制

---

### F-009 通知过滤

| 属性 | 描述 |
|------|------|
| 优先级 | P2 |
| 所属模块 | 设置 |

**功能描述：**
过滤不需要在面板中显示的会话，减少干扰。支持：
- 内置规则（如 Codex Memory Consolidation、Claude-Mem 后台会话等）
- 按工作目录过滤（隐藏包含特定路径片段的会话）
- 按首条提示词前缀过滤

**验收标准：**
1. 内置规则默认开启，可单独关闭
2. 自定义目录过滤：输入路径片段，匹配的会话不显示
3. 过滤规则即时生效，已显示的会话应消失
4. 提供"右键会话卡片 → 加入过滤"的快捷操作

---

### F-010 声音提醒

| 属性 | 描述 |
|------|------|
| 优先级 | P2 |
| 所属模块 | 设置 |

**功能描述：**
在特定状态转换时播放提示音：
- 会话开始
- 任务完成
- 任务错误
- 需要审批
- 任务确认（用户发送了消息）
- 上下文限制（上下文窗口即将占满）
- 闲置提醒

每个事件可独立配置提示音音色和音量。

**验收标准：**
1. 提供全局音量控制和启用/禁用开关
2. 每个事件类型可独立选择音色
3. 播放时不阻塞 UI
4. 静音模式下不发声

---

### F-011 设置面板

| 属性 | 描述 |
|------|------|
| 优先级 | P1 |
| 所属模块 | 设置 |

**设置分类：**

| 分类 | 包含项 |
|------|--------|
| 通用 | 登录时打开、悬停延迟、智能抑制、全屏时隐藏、鼠标离开自动收起、自动提醒停留时长、空闲会话自动清理时间、禁用点击跳转 |
| 集成 | 各 Agent 启用/禁用、终端选择 |
| 通知 | 内置过滤规则、自定义过滤（目录/提示词前缀） |
| 显示 | 简洁/详细模式、显示器选择、面板字体大小、完成卡片高度、最大面板高度/宽度、刘海宽高微调、子代理活动详情显示 |
| 声音 | 各事件音效配置 |
| 快捷键 | 全局快捷键绑定 |

**验收标准：**
1. 设置面板为独立窗口，左侧导航 + 右侧内容
2. 所有设置项即时生效（无需重启）
3. 设置持久化到本地（UserDefaults 或 plist 文件）

---

## 3. 用户使用流程

### 3.1 首次启动流程

```
用户双击 App 图标
  → 请求辅助功能权限（用于窗口跳转）
  → 引导选择已安装的 Agent（自动检测 + 手动配置）
  → 引导选择常用终端（iTerm2 / Terminal / Ghostty）
  → 完成设置，缩小态出现在刘海区
```

### 3.2 日常使用流程 - 被动监控

```
用户在使用其他应用（浏览器/文档编辑器）
  → 刘海区缩小态实时显示 Agent 状态（"执行中"、"2 个会话"）
  → 用户余光可见状态变化
  → Agent 完成任务 → 缩小态图标变绿/变橙
  → 如需确认 → 面板自动展开
  → 用户扫一眼面板内容
  → 点击 Allow / 选择选项
  → 面板收起，Agent 继续工作
  → 用户继续当前工作，无需切换窗口
```

### 3.3 日常使用流程 - 主动查看

```
用户好奇当前 Agent 进展
  → 鼠标移到刘海区
  → 0.15s 后面板展开，显示所有活跃任务
  → 查看各任务状态和进度
  → 如需查看详情 → 点击任务卡片 → 跳转到对应终端
  → 如不需要 → 鼠标移开 → 面板收起
```

### 3.4 多 Agent 排队确认流程

```
Agent A 完成任务，返回确认请求
  → 面板自动展开，展示 Agent A 的确认项
  → 此时 Agent B 也完成任务，入确认队列
  → 用户确认 Agent A 的请求
  → 面板内容无缝切换到 Agent B 的确认项（不收起）
  → 用户确认 Agent B
  → 队列为空，面板收起
```

---

## 4. 页面/视图清单

| 编号 | 视图名称 | 描述 | 触发方式 |
|------|---------|------|---------|
| V-001 | 缩小态 Bar | 刘海区常驻紧凑条 | 默认显示 |
| V-002 | 扩展态 - 任务列表 | 所有活跃任务的列表面板 | hover 触发 |
| V-003 | 扩展态 - 权限审批 | diff 预览 + Deny/Allow | Agent 请求权限时 |
| V-004 | 扩展态 - 选择问题 | 选项列表 + 快捷键 | Agent 提问时 |
| V-005 | 扩展态 - 完成通知 | 输入摘要 + 输出摘要 + 确认项 | 任务完成时 |
| V-006 | 设置窗口 | 左导航 + 右内容的配置面板 | 菜单栏图标点击 |
| V-007 | 首次引导 | Agent 检测 + 终端选择 | 首次启动 |

---

## 5. 非功能需求

| 需求 | 标准 |
|------|------|
| 启动时间 | 冷启动 < 2 秒 |
| 内存占用 | 常驻 < 80MB |
| CPU 占用 | 空闲时 < 1%，轮询时 < 3% |
| 状态延迟 | Agent 状态变化到 UI 更新 < 3 秒 |
| 动画帧率 | 展开/收起动画 60fps |
| 系统兼容 | macOS 14.0+ (Sonoma)，支持有刘海的 MacBook |
| 数据安全 | 所有数据本地处理，不上传任何用户代码/对话到外部服务器 |
| 无障碍 | 支持 VoiceOver 基础读取 |

---

## 6. 不做的事情（Scope 边界）

- 不做 Agent 的完整对话界面（只做状态展示和确认交互）
- 不做代码编辑器功能
- 不做 Agent 的配置管理（Agent 自身的配置在各自的产品中完成）
- 不做云端同步
- 不做 Windows/Linux 版本（MVP 阶段仅 macOS）
- 不做 AI 摘要（直接截断展示 Agent 输出，不调用额外 AI API）
